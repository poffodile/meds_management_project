# Clients Reference Source

Reference package saved here:

- `docs/care-one-os/FRONTEND4/care-one-os-clients-source-reference.zip`

This ZIP was provided on 9 August 2026 as the exact design and workflow reference for the Care One OS Clients section.

Implementation rules:

- Preserve the Clients directory and Client profile layout, tabs, responsive behaviour, and workflows where they fit Frontend4.
- Adapt into the existing Laravel, React, Inertia, and Frontend4 styling surface only.
- Do not affect Frontend1, Frontend2, or Frontend3.
- Do not copy prototype clinical data from the reference package into the live app.
- Use existing backend models/API data wherever present.
- Use explicit placeholders such as `Not recorded` or `Not structured` where the existing backend does not yet provide a field.
